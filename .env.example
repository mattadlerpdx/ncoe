# NCOE Case Management System - Environment Configuration

# Server Configuration
SERVER_ADDRESS=:8080

# Database (leave empty for demo mode with mock data)
DATABASE_URL=

# Environment (development or production)
ENVIRONMENT=development

# Session secret (generate a random 32-character string for production)
SESSION_SECRET=change-this-in-production-please

# Base URL for redirects
BASE_URL=http://localhost:8080

# Branding config file location
BRANDING_CONFIG=config/branding.yaml

# Email Configuration (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=ncoe@ethics.nv.gov
